{
  "name": "@miciodev/event-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @miciodev/shared-types build && tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@miciodev/shared-types": "workspace:*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14"
  }
}
